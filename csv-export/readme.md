Export Events from Opencast to a csv file
=========================================

1. Edit credentials in `getEvents.sh`
2. Run `./getEvents.sh` to get a JSON file which contains all events
3. Run `./events2csv.py` to generate the CSV file
4. Open `events.csv`
